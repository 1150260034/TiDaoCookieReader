<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TiDaoCookieReader" parent="android:Theme.Material.Light.NoActionBar">
        <!-- 暗色主题基础色 -->
        <item name="android:windowBackground">@color/color_background</item>
        <item name="android:textColorPrimary">@color/color_primary_text</item>
        <item name="android:textColorSecondary">@color/color_secondary_text</item>
        <item name="android:colorBackground">@color/color_background</item>
        <item name="android:colorPrimary">@color/color_primary</item>
        <item name="android:colorAccent">@color/color_primary</item>
        <item name="android:statusBarColor">@color/color_background</item>
        <item name="android:navigationBarColor">@color/color_background</item>
        <!-- AlertDialog 使用暗色主题，避免白底白字 -->
        <item name="android:alertDialogTheme">@style/AppDialog</item>
    </style>

    <!-- 暗色对话框样式 -->
    <style name="AppDialog" parent="android:Theme.Material.Dialog.Alert">
        <item name="android:colorPrimary">@color/color_primary</item>
        <item name="android:colorAccent">@color/color_primary</item>
    </style>
</resources>